# Agent Configuration

## Vite Development Server

The Vite dev server for the Rod web UI is configured to run on **port 60000**.

**Location:** `web/vite.config.ts`

To start the development server:
```bash
cd web && npm run dev
```

The app will be available at `http://localhost:60000`
