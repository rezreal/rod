image boot.vfat {
	vfat {
		files = {
			"rpi-firmware/config.txt",
			"rpi-firmware/overlays",
			"bcm2712-rpi-5-b.dtb",
			"Image",
		}
	}
	size = 64M
}

image sdcard.img {
	hdimage {
	}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		image = "boot.vfat"
	}

	partition rootfs {
		partition-type = 0x83
		image = "rootfs.ext4"
	}
}
